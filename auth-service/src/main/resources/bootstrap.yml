
spring:
  cloud:
    config:
      uri: http://localhost:8761
  application:
    name: auth-service
server:
  port: 8090
  context-path: /uaa
  use-forward-headers: true

security:
  basic:
    enabled: false
  enable-csrf: false